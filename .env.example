# Скопируй в .env. Используется docker-compose для подстановки в build args.
# Эти значения инлайнятся в клиентский бандл при сборке (NEXT_PUBLIC_*).

NEXT_PUBLIC_API_URL=https://backend.migntender.app/api/v1
NEXT_PUBLIC_DADATA_TOKEN=replace-me
